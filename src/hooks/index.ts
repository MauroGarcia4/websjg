/**
 * Exportaciones centralizadas de hooks personalizados
 */

export {
  useScrollAnimation,
  useStaggeredAnimation,
  useSmoothScroll,
  useScrollPosition,
} from './useScrollAnimation';

export {
  useContactForm,
} from './useContactForm';

export type {
  ContactFormData,
  ContactFormErrors,
} from './useContactForm';
